<span
    class="absolute -top-2 -right-2 inline-flex min-w-[20px] items-center justify-center rounded-full bg-accent px-1.5 py-0.5 text-[10px] font-bold text-white shadow {{ $count > 0 ? '' : 'hidden' }}"
    aria-hidden="{{ $count > 0 ? 'false' : 'true' }}"
>
    {{ $count }}
</span>
