<span
    class="ml-2 inline-flex min-w-[18px] items-center justify-center rounded-full bg-accent px-1.5 py-0.5 text-[10px] font-bold text-white {{ $count > 0 ? '' : 'hidden' }}"
    aria-hidden="{{ $count > 0 ? 'false' : 'true' }}"
>
    {{ $count }}
</span>
